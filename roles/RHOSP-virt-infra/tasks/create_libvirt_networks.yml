- name: NETWORKS - Create libvirt hooks directory
  file:
    path: "/etc/libvirt/hooks"
    state: directory
    mode: "755"
    owner: root
    group: root
  when: inventory_hostname == 'localhost'

- name: NETWORKS - Add hook for libvirt networks (hack) in order to allow port-forwarding when the network is forwarding route
  template:
    src: "network-hook.j2"
    dest: "/etc/libvirt/hooks/network"
    mode: "755"
    owner: root
    group: root
  become: yes
  notify: HANDLER - restart libvirtd service
  when:
    - inventory_hostname == 'localhost'
    # This libvirt port-forward hook is only relevant when the External network is NAT.
    # In the leaf model every leaf bridge (ctlplane and additional_bridges, incl.
    # br-external) is wired into libvirt_networks as forward:bridge, so this guard is
    # false and the hook stays disabled - port forwarding to the overcloud is handled by
    # the firewalld rich rules in configure_firewall.yml. The guard still fires the hook
    # automatically should a NAT-mode external network ever be added.
    - (libvirt_networks | selectattr('name', 'search', 'external') | selectattr('forward', 'equalto', 'nat') | list | length) > 0

- name: NETWORKS - Gather facts about libvirt networks
  community.libvirt.virt_net:
    command: facts

- name: NETWORKS - Define the network from the template
  community.libvirt.virt_net:
    command: define
    name: "{{ item.name }}"
    xml: "{{ lookup('template', 'network.xml.j2') }}"
  loop: "{{ libvirt_networks }}"
  become: yes
  when:
    - inventory_hostname == item.hypervisor
    - ansible_libvirt_networks[item.name] is not defined

- name: NETWORKS - Gather facts about libvirt networks again
  community.libvirt.virt_net:
    command: facts

- name: NETWORKS - Activate the network
  community.libvirt.virt_net:
    state: active
    name: "{{ item.name }}"
  loop: "{{ libvirt_networks }}"
  become: yes
  when:
    - inventory_hostname == item.hypervisor
    - ansible_libvirt_networks[item.name]['state'] != 'active'

- name: NETWORKS - Enable autostart for the network
  community.libvirt.virt_net:
    autostart: yes
    name: "{{ item.name }}"
  loop: "{{ libvirt_networks }}"
  become: yes
  when:
    - inventory_hostname == item.hypervisor
    - ansible_libvirt_networks[item.name]['autostart'] != 'yes'
