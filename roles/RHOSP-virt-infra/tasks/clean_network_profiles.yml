# Cleanup is deliberately NOT gated on bridges_required/dcn_leafs: it must remove
# whatever a previous run may have created even if the vars changed since. Marking an
# already-absent connection `persistent_state: absent` is an idempotent no-op.

# Remove the VLAN sub-interfaces first (children of the bridges). The create side names
# them "<bridge>.<vlan>"; mirror that exactly so they are actually deleted - otherwise
# they are orphaned in NetworkManager when only the parent bridge is removed.
- name: NETWORKING - Delete vlans belonging to libvirt bridges in localhost
  set_fact:
    network_connections: "{{ (network_connections | default([])) + [connection] }}"
  vars:
    connection:
      name: "{{ item.bridge }}.{{ item.subnet.vlan }}"
      type: vlan
      state: down
      persistent_state: absent
  when:
    - inventory_hostname == 'localhost'
    - item.subnet.vlan is defined
  loop: "{{ default_leaf.networks }}"

- name: NETWORKING - Delete vlans belonging to libvirt bridges in the leafs
  set_fact:
    network_connections: "{{ (network_connections | default([])) + [connection] }}"
  vars:
    connection:
      name: "{{ item.1.bridge }}.{{ item.1.subnet.vlan }}"
      type: vlan
      state: down
      persistent_state: absent
  when:
    - item.0.hypervisor == inventory_hostname
    - item.1.subnet.vlan is defined
  with_subelements:
    - "{{ dcn_leafs }}"
    - networks

- name: NETWORKING - Delete bridges for networks linked with the hypervisor
  set_fact:
    network_connections: "{{ (network_connections | default([])) + [connection] }}"
  vars:
    connection:
      name: "{{ item.bridge }}"
      interface_name: "{{ item.bridge }}"
      type: bridge
      state: down
      persistent_state: absent
  when:
    - inventory_hostname == item.hypervisor
    - item.forward == 'bridge'
  loop: "{{ networks }}"

- name: NETWORKING - Delete hypervisor interfaces
  set_fact:
    network_connections: "{{ (network_connections | default([])) + [connection] }}"
  vars:
    connection:
      name: "{{ item.hypervisor_if }}"
      interface_name: "{{ item.hypervisor_if }}"
      type: ethernet
      state: down
      persistent_state: absent
  when:
    - inventory_hostname == item.hypervisor
    - (item.forward == 'bridge') and (item.hypervisor_if is defined)
  loop: "{{ networks }}"

- name: NETWORKING - Define only the network configuration for the public interface of the DCN hypervisors without static routes
  set_fact:
    network_connections: "{{ (network_connections | default([])) + [connection] }}"
  vars:
    connection:
      name: "{{ hypervisor_external_if }}"
      interface_name: "{{ hypervisor_external_if }}"
      type: ethernet
      autoconnect: yes
      state: up
      ip:
        dhcp4: yes
  when: inventory_hostname != 'localhost'

- name: NETWORKING - Define only the network configuration for the public interface of the localhost hypervisor without static routes
  set_fact:
    network_connections: "{{ (network_connections | default([])) + [connection] }}"
  vars:
    connection:
      name: "{{ external_if }}"
      interface_name: "{{ external_if }}"
      type: ethernet
      autoconnect: yes
      state: up
      ip:
        dhcp4: yes
  when: inventory_hostname == 'localhost'

- name: NETWORKING - Configure network using system role
  import_role:
    name: rhel-system-roles.network
  vars:
    network_allow_restart: yes

- name: NETWORKING - Gather facts again to get interfaces
  setup: