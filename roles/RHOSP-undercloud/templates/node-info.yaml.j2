parameter_defaults:
{% for ocrole in overcloud_roles %}
  Overcloud{{ ocrole.name }}Flavor: baremetal
  {{ ocrole.name }}Count: {{ vms | selectattr("profile", "equalto", ocrole.profile) | list | count }}
{% endfor %}
