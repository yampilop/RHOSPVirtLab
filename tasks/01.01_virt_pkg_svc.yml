- name: PACKAGES - Ensure packages are installed
  package:
    name:
      - libvirt
      - guestfs-tools
      - virt-manager
    state: latest
  become: yes

- name: PACKAGES - Ensure libvirt socket is enabled and running
  systemd:
    name: libvirtd.socket
    state: started
    enabled: true
  become: yes
